package rasterops;

import objectdata.Polygon2D;
import rasterdata.RasterImage;

public class Polygoner2D<P> {

    public void drawPolygon(final RasterImage<P> image, final P pixel, final Liner<P> liner, final Polygon2D polygon) {
        /*int listSize = polygon.getPoint().size();
        for(int i = 0; i < listSize; i++) {
            int next = (i + 1) % listSize;
            liner.drawLine(image,polygon.);
        }*/
    }
}
